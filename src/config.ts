// ============================
// App Configuration
// ============================
// Toggle this to switch from mock/localStorage to Supabase repositories.
// When true, GarageContext will use SupabaseGarageRepository instead.

export const USE_SUPABASE = false;
